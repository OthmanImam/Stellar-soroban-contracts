[package]
name = "did-contract"
version = "0.1.0"
edition = "2021"

[lib]
name = "did_contract"
path = "src/lib.rs"

[dependencies]
soroban-sdk = { workspace = true }
shared = { path = "../shared" }
authorization = { path = "../authorization" }
