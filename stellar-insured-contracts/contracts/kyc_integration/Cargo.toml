[package]
name = "kyc-integration"
version = "0.1.0"
edition = "2021"

[lib]
name = "kyc_integration"
path = "src/lib.rs"

[dependencies]
soroban-sdk = { workspace = true }
shared = { path = "../shared" }
authorization = { path = "../authorization" }
did-contract = { path = "../did" }
